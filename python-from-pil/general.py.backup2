import re

def line_split(line):
        return re.findall(r'[^"\s]\S*|".+?"', line) # magic from the internet

code = "(pow 3 2)"

a='(2 "slaw choni")'
new=""
in_quote=False
for c in a:
    if c=="\"": in_quote=not(in_quote)
    if in_quote and c==" ":
        new = new+"];["
    else: new = new+c
new=new.replace(' ',' , ').replace('(',' [ ').replace(')',' ] ').split()
for i,v in enumerate(new):
    if v[0]=="\"": new[i] = v.replace("];["," ")
